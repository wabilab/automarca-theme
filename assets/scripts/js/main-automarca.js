import 'bootstrap/dist/js/bootstrap';

jQuery(function ($) {
/*     $.ajax({
        method: "GET",
        url: /wp/v2/load-more/' + page,
    }).done(function(response) { */
        console.log(home_url);
})  